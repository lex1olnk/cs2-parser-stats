{
  "name": "cs-parser",
  "version": "1.0.0",
  "description": "CS2 demo parsing service",
  "main": "index.js",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "start": "node --env-file=.env index.js",
    "dev": "nodemon --exec \"node --env-file=.env index.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@laihoe/demoparser2": "^0.39.1",
    "@prisma/client": "^6.17.1",
    "adm-zip": "^0.5.16",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
